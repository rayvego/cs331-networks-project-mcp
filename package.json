{
	"name": "networks-project-mcp",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"build": "cd packages/mcp-server && bun run build && cd ../mcp-client && bun run build",
		"start": "bun install && bun run build && cd packages/mcp-client && bun run start"
	}
}
